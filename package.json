{
  "name": "remote-cli-agents",
  "version": "0.1.0",
  "private": true,
  "description": "Web-based remote control for CLI coding agents (Claude Code, Codex, Gemini CLI)",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "dev": "npm run dev:server & npm run dev:web",
    "dev:server": "npm run dev -w packages/server",
    "dev:web": "npm run dev -w packages/web",
    "build": "npm run build -w packages/shared && npm run build -w packages/server && npm run build -w packages/web",
    "start": "npm run start -w packages/server"
  },
  "license": "MIT"
}
